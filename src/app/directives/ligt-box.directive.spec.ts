import { LigtBoxDirective } from './ligt-box.directive';

describe('LigtBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new LigtBoxDirective();
    expect(directive).toBeTruthy();
  });
});
