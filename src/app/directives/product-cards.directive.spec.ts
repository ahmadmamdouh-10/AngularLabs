import { ProductCardsDirective } from './product-cards.directive';

describe('ProductCardsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductCardsDirective();
    expect(directive).toBeTruthy();
  });
});
