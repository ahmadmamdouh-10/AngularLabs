import { EgyNationalIdPipe } from './egy-national-id.pipe';

describe('EgyNationalIdPipe', () => {
  it('create an instance', () => {
    const pipe = new EgyNationalIdPipe();
    expect(pipe).toBeTruthy();
  });
});
